node_modules/
dist/
data/
_resources/

# dotenvx — private decryption keys must never be committed
.env.keys
.env.keys.*

# Optional plaintext local overrides (not encrypted)
.env.local
.env.*.local

# Unused local Inter font dump (app uses @fontsource-variable)
assets/inter/

# Test coverage (generated locally; do not commit HTML/XML/JSON reports)
coverage/

# Playwright
/playwright-report/
/test-results/
/blob-report/
/playwright/.cache/

